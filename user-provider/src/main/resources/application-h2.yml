spring:
  datasource:
    platform: h2
#    schema: classpath:schema.sql
    schema: classpath:data.sql
  h2:
    console:
      enabled: true
      path: /h2-console
      settings:
        web-allow-others: true